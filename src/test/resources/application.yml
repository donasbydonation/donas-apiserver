spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:boost
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      show_sql: true
      format_sql: true